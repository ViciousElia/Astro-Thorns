---
import '../styles/navigation.css';

const { nav, chapter, second } = Astro.props;
---
<div class={`story-navigation${second ? ' bottom' : ''}`}>
  {nav.first==chapter?
    <span> &lt; &lt; </span>:
    <span><a href={`/p/${nav.first}`}> &lt; &lt; </a></span>
  }
  {nav.prev==chapter?
    <span> &lt; </span>:
    <span><a href={`/p/${nav.prev}`}> &lt; </a></span>
  }
  {nav.next==chapter?
    <span> &gt; </span>:
    <span><a href={`/p/${nav.next}`}> &gt; </a></span>
  }
  {nav.last==chapter?
    <span> &gt; &gt; </span>:
    <span><a href={`/p/${nav.last}`}> &gt; &gt; </a></span>
  }
</div>